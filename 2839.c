/*
#include <stdio.h>

int main(void)
{
    int n;
    scanf("%d", &n);

    int count = 0;
    while (1)
    {
        if (n % 5 == 0) { count = count + (n / 5); break; }
        if (n >= 3) { n -= 3; count++; continue; }
        count = -1;
        break;
    }

    printf("%d", count);

    return 0;
}
*/
